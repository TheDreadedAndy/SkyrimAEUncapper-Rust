[package]
name = "skyrim_uncapper"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
skse64 = { path = "../skse64" }
versionlib = { path = "../versionlib" }
